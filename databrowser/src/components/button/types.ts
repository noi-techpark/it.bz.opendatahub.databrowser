export enum Variant {
  solid = 'solid',
  ghost = 'ghost',
}

export enum PillVariant {
  rounded = 'rounded',
  edged = 'edged',
}

export enum Size {
  md = 'md',
}

export enum Tone {
  primary = 'primary',
}
