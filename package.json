{
    "name": "databrowser",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "bootstrap": "npm install && lerna bootstrap"
    },
    "devDependencies": {
        "lerna": "^4.0.0"
    }
}