{
  "name": "databrowser-monorepo",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "bootstrap": "npm install && lerna bootstrap",
    "release-components": "lerna publish"
  }
}
